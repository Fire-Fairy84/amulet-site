---
import { getCollection } from 'astro:content';
import GuideIndex from '../../../components/GuideIndex.astro';
const guides = await getCollection('guides', ({ data }) => data.language === 'en');
---
<GuideIndex language="en" guides={guides} />
